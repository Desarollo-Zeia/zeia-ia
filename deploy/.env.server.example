# --- OpenRouter (LLM) ---
OPENROUTER_API_KEY=

# --- Base de datos: conexión DIRECTA (misma VPC, sin túnel SSH) ---
DB_HOST=172.31.29.136
DB_PORT=5432
DB_USER=postgres
DB_NAME=energy
DB_PASSWORD=

# --- API del agente (Fase 1: CORS + API key + voz) ---
AGENT_API_KEY=
CORS_ORIGINS=http://localhost:5173
ELEVENLABS_API_KEY=

# Puerto local de uvicorn (nginx expone 80/443)
PORT=8000
